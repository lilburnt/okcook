`okcook` is a browser extension that reads a recipe from a page you are looking at and turns it into ingredients, steps and a shopping list.

This repo hosts the privacy policy for the browser extension. 